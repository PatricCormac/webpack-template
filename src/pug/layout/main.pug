block variables

doctype html
html(lang='en')
  head
    meta(charset='utf-8')
    title #{title}
    meta(name='description', content=description)
  body(class=pageClass)
    include ../includes/modules/header
    block content